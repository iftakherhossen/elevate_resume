import React from 'react';
import { Layout, SectionWrapper } from '../components';

const CreateResume = () => {
    return (
        <Layout resume={true}>
            <SectionWrapper />
        </Layout>
    );
};

export default CreateResume;